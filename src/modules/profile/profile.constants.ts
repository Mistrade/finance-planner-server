export const HASH_PASSWORD_SALT_VALUE = 8;

export enum PROFILE_SERVICE_MESSAGES {
  USER_ALREADY_EXISTS = 'Пользователь с такими данными уже существует',
  USER_NOT_FOUND = 'Пользователь не найден',
}

export enum SYSTEM_ROOTS {
  CLIENT = 'client',
  ADMIN = 'admin',
}
